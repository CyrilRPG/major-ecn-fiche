"""Major ECN Generator — transformation de PDF de cours en fiches de révision.

Pipeline : extraction PDF → rédaction IA (3 étapes) → analyse d'images (vision)
→ assemblage → rendu HTML → export PDF + DOCX.
"""

__version__ = "1.0.0"
